"""Ejentic lead-gen — reusable safety core.

A small, dependency-free toolkit that turns the prototype into a system safe to
point at real people: per-client config, idempotent state, a suppression gate,
CAN-SPAM compliance, and a throttled/capped sender.

No heavy frameworks (Engineering rule 1) — standard library only, plus libs
already in requirements.txt.
"""
